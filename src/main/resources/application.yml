spring:
   datasource:
      url: jdbc:mysql://localhost:3306/estacionamiento?useSSL=false
      username: root
      password: ceiba
   jpa:
      show-sql: true
      hibernate:
         ddl-auto: update
      properties:
         hibernate:
         dialect: org.hibernate.dialect.MySQL5InnoDBDialect
      database-platform: org.hibernate.dialect.MySQL5InnoDBDialect